{
  "private": true,
  "name": "api.frontendista.cz",
  "description": "TODO: Change (A template for kick starting a Cloudflare Workers project)",
  "main": "build/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild app/index.ts --bundle --minify --outfile=build/index.js",
    "check:tsc": "tsc -noEmit",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Pavel Sušický <susicky.pavel@outlook.cz>",
  "license": "MIT",
  "dependencies": {
    "nope-validator": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "esbuild": "^0.14.30",
    "prettier": "^1.18.2",
    "typescript": "^4.6.3"
  }
}
