{
  "private": true,
  "name": "api.frontendista.cz",
  "description": "TODO: Change (A template for kick starting a Cloudflare Workers project)",
  "main": "build/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild index.js --bundle --minify --outfile=build/index.js",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Pavel Sušický <susicky.pavel@outlook.cz>",
  "license": "MIT",
  "dependencies": {
    "nope-validator": "^1.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.30",
    "prettier": "^1.18.2"
  }
}
